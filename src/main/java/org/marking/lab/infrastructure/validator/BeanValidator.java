package org.marking.lab.infrastructure.validator;

import org.marking.lab.application.Validator;

public class BeanValidator implements Validator {

	@Override
	public <T> void validate(T object) {
		
	}
}
